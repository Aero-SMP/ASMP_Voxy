# Run from the server directory: ./world is the source, ./voxy-data is generated data.
rayon_threads = 0

[quic]
# Empty resolves to --minecraft-port + 200 on startup (25765 without the option).
# The Java controller supplies the current server.properties server-port each launch.
listen = ""
advertise_host = ""
advertise_port = 0
